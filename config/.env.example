# MacPilot environment variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   chmod 600 .env
#
# These are injected into every agent's environment.
# Variables already set (via plist or inline override) are not overwritten.

# --- Notifications ---
# Push notifications via ntfy.sh (required for headless deployments)
# NTFY_TOPIC=your-topic-name
# NTFY_SERVER=https://ntfy.sh
# Set to false to disable all notifications (ntfy + osascript)
# MACPILOT_NOTIFY=false

# --- API Keys ---
# BUGSNAG_API_KEY=your-key-here
# BUGSNAG_PROJECT_ID=your-project-id
# GITHUB_TOKEN=your-token-here
# LINEAR_API_KEY=your-key-here

# --- Project Settings ---
# Default project directory (used by agents that cd into a project)
# PROJECT_DIR=~/Developer/MyApp
# TEST_PLAN=AllTargets
# SIMULATOR_DESTINATION=platform=iOS Simulator,name=iPhone 16,OS=latest
# GitHub repo for triage-github agent (owner/repo format)
# GITHUB_REPO=owner/repo

# --- Log Rotation ---
# MACPILOT_LOG_RETENTION_DAYS=30
